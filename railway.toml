[deploy]
startCommand = "yarn workspace bike-tracker-back start"
numReplicas = 1
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 1

[deploy.limitOverride.containers]
cpu = 0.1
memoryBytes = 536870912 # 512 MB